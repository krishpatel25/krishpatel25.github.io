# Portfolio Instructions

Keep `CLAUDE.md`, `CODEX.md`, and `AGENT.md` synchronized. Any material change to one must be applied to all three.

- Build Krish Patel's CMU 15-113 hardware-engineering portfolio.
- Visual reference: `krish-patel.com`. Keep its dark, narrow, personal editorial style; avoid generic card grids.
- Story: systems foundations → RISC-V architecture (flagship) → silicon flow → hardware + AI.
- Use project-specific visuals and only purposeful interactions.
- Keep `index.html`, `styles.css`, and `script.js` separate and readable.
- Maintain `prompt-log.txt` with every material prompt. Record the prompt only.
  Do not record AI responses, result summaries, or file diffs — the log is a
  record of what was asked, not of what was produced.
- Write each prompt entry as specifically as the prompt was actually given:
  number it, date it, and keep the files, constraints, and decisions it named.
  Do not compress a detailed prompt into a vague one-liner.
- Append a new entry in the same turn as the work it asked for, so the log
  never falls behind.
- Do not create a GitHub repository, deploy, change DNS, or update the live site without explicit approval.
